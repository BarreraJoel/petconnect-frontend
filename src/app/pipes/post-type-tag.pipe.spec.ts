import { PostTypeTagPipe } from './post-type-tag.pipe';

describe('PostTypeTagPipe', () => {
  it('create an instance', () => {
    const pipe = new PostTypeTagPipe();
    expect(pipe).toBeTruthy();
  });
});
