import { UserTypeTagPipe } from './user-type-tag.pipe';

describe('UserTypeTagPipe', () => {
  it('create an instance', () => {
    const pipe = new UserTypeTagPipe();
    expect(pipe).toBeTruthy();
  });
});
